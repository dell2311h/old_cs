# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  hello: "Hello world"
  errors:
    parameters:
      empty_videos: "Not defined parameter 'videos' for search recommended events"
      empty_count:  "Count parameter not defined"
      empty_name:   "Name parameter not defined"

  feed:
    user:
      follow: '$actor$ started following $entity$'
      tagging: '$actor tagged $entity$ in $context$'
      comment_video: "$actor commented video $entity$"
      video_upload: "$actor uploaded $entity$"
    news:
    notification:
    place:
      tagging: 'Place $entity$ was tagged by $actory$ in $context$ comment'
    event:
      tagging: 'Event $entity$ was tagged by $actory$ in $context$ comment'
    performer:

